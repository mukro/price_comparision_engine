
# --- monitoring & maintenance ---
pandas>=2.2.0
pyarrow>=15.0.0
boto3>=1.34.0

# --- compliance (robots.txt parsing) ---
protego>=0.3.0

# --- proxy (optional, for local testing) ---
# traefik is a binary, not a Python package
# nginx is a system package
